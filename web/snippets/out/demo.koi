let me = $(whoami).<span class="fn">strip</span>()
echo Greetings {me}!

for _, n in [1 2 3] {
    <span class="fn">print°(<span class="str">'2 to the °{n}<span class="str"> is </span>{2^n}</span>2'</span>)
}

[
    {cmd: <span class="str">'ls'</span>}
    {cmd: <span class="str">'ps'</span>}
].<span class="fn">forEach</span>(fn(obj){
    man {obj.cmd} --help <span class="kw">|</span> head -n 5
})